// subair aggregatable value gprod;

air value(prod) pepe,pepe_all_prods;

air value pepe_all_prods;
subair value pepe;

pepe_all_prods = 1, pepe_prods_prods = pepe_prods_prods * pepe;

pepe_all_prods = repeat(pepe, 1,)

foreach(pepe, 1, self*pepe);

each(1, self * pepe) === 1;
each(0, pepe_prods_prods + pepe) === 1;
each(0, pepe_prods_prods + ch1[3] * pepe) === 1;
