namespace Main::Example {

challenge zeta stage(2);
challenge alpha stage(2);

public myTableNumRows;
public myTable[2][10]; // 2 columns, 10 rows
prover myTableValue;

 public_table_prod(myTableValue, myTable, myTableNumRows, zeta, alpha);

}